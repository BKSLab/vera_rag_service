[pytest]
asyncio_mode = auto
addopts = -m "not slow"
markers =
    slow: нагрузочные/масштабные тесты на синтетическом крупном корпусе (TEST-3) — не входят в обычный прогон/CI, запускать явно: pytest -m slow tests/performance
