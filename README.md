# vera_rag_service

RAG Service — сервис семантического поиска по базе знаний для AI-ассистента «Вера» (консультант по правам людей с инвалидностью в сфере трудоустройства, проект «Работа для всех»).

## Роль в системе

Один из трёх сервисов архитектуры ассистента: **Agent Service** (оркестратор диалога) → **MCP Tools Server** (прослойка инструментов) → **RAG Service** (этот репозиторий). RAG Service принимает HTTP-запросы от MCP Tools Server и не взаимодействует с пользователем напрямую.

База знаний: нормативные документы (ТК РФ, ФЗ-181 «О социальной защите инвалидов», подзаконные акты) и авторские статьи-систематизации.

## Что делает сервис

- **Ingestion** — препроцессинг документов, иерархический чанкинг (документ → секция → чанк), обогащение чанков синтетическими заголовками и гипотетическими вопросами (LLM), эмбеддинг и сохранение в Qdrant
- **Search** — гибридный поиск (dense + sparse/BM25 + RRF fusion) с последующим переранжированием cross-encoder reranker'ом
- **API** — `POST /search`, `POST /ingest`, `DELETE /document/{id}`, `GET /health`

## Стек

FastAPI · Qdrant (self-hosted, векторное хранилище) · embedding-модель (конфигурируется) · cross-encoder reranker (`BAAI/bge-reranker-base` или аналог) · OpenTelemetry/openinference → Arize Phoenix

## Документация

- [`AGENT_VERA_ARCHITECTURE.md`](AGENT_VERA_ARCHITECTURE.md) — архитектура всей системы из трёх сервисов
- [`AGENT_VERA_WBS.txt`](AGENT_VERA_WBS.txt) — иерархическая структура работ проекта
- [`RAG_SERVICE_PLAN.md`](RAG_SERVICE_PLAN.md) — план реализации этого сервиса по этапам

## Статус

Концепция / начало разработки. См. план реализации для деталей этапов и зависимостей.
